# Simple_Interpreter
This project implements an interpreter that executes instructions and print the result of  execution. Therefore, the program accepts a text file as source code, and then parses the input  file and prints the value of each variable.
